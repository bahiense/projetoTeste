<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Acesso a todos os arquivos. É o que permite varrer o cartão inteiro em
      vez de só a pasta do próprio app. A Play Store restringe muito essa
      permissão, mas para um app instalado por APK ela funciona normalmente.
    -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Necessária para ler quanto cada app instalado ocupa. -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Faxina">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Serviço que aperta o botão "Limpar cache" dentro de Configurações.
          Fica desligado até o usuário ligar à mão; o packageNames do XML
          restringe o que ele recebe às telas de Configurações, e nada mais.
          exported precisa ser true para o sistema conseguir vinculá-lo, e a
          permissão de bind garante que só o sistema faça isso.
        -->
        <service
            android:name=".FaxineiroAcessivel"
            android:exported="true"
            android:label="@string/acessibilidade_nome"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/acessibilidade" />
        </service>

        <!--
          Abrir um arquivo em outro app exige uma content:// — desde o Android 7
          passar um file:// direto derruba o processo com FileUriExposedException.
          O provedor é fechado (exported=false) e cada abertura concede permissão
          de leitura só para aquela URI.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.arquivos"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/arquivos_visiveis" />
        </provider>
    </application>

</manifest>
