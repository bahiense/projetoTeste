name: Gerar APK do Segundo Antes

on:
  push:
    # também nas branches de trabalho: é o único jeito de saber se compila
    # antes de o código chegar no master
    branches: [master, 'claude/**']
    paths:
      - 'gatilhos/**'
      - 'gatilhos-android/**'
      - '.github/workflows/segundo-antes-apk.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4

      - name: Compilar o APK
        working-directory: gatilhos-android
        run: ./gradlew assembleDebug --stacktrace

      - name: Renomear o arquivo
        run: |
          mkdir -p dist
          cp gatilhos-android/app/build/outputs/apk/debug/app-debug.apk dist/segundoantes.apk
          ls -lh dist/

      # a assinatura precisa ser a mesma em toda build, senão o Android recusa
      # a atualização com "app não instalado"
      - name: Conferir a assinatura
        run: |
          APKSIGNER=$(ls "$ANDROID_HOME"/build-tools/*/apksigner | sort -V | tail -1)
          "$APKSIGNER" verify --print-certs dist/segundoantes.apk | grep -i "SHA-256 digest" || true

      - name: Guardar o APK no workflow
        uses: actions/upload-artifact@v4
        with:
          name: segundoantes-apk
          path: dist/segundoantes.apk
          if-no-files-found: error

      # link fixo e público para baixar direto no celular — só do master, para
      # build de branch não trocar o APK que está publicado
      - name: Publicar na aba Releases
        if: github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: softprops/action-gh-release@v2
        with:
          tag_name: segundoantes-apk-latest
          name: O Segundo Antes APK
          body: |
            APK do Segundo Antes, gerado automaticamente a partir do código deste repositório.

            Baixe o `segundoantes.apk` pelo celular e abra o arquivo para instalar.
            O Android vai avisar que o app não veio da Play Store — é esperado, já que
            o arquivo foi compilado aqui mesmo.

            O app funciona sem internet e não pede nenhuma permissão para funcionar.
            A de notificação só é pedida se você ligar o lembrete diário.
          files: dist/segundoantes.apk
          fail_on_unmatched_files: true

      # tag separada, para dar um link de instalação antes de o trabalho chegar
      # no master sem mexer no APK publicado
      - name: Publicar prévia da branch
        if: startsWith(github.ref, 'refs/heads/claude/')
        continue-on-error: true
        uses: softprops/action-gh-release@v2
        with:
          tag_name: segundoantes-apk-preview
          name: O Segundo Antes APK (prévia)
          prerelease: true
          body: |
            Prévia compilada da branch de trabalho, para instalar e testar antes de
            mesclar no master. Baixe o `segundoantes.apk` pelo celular e abra o
            arquivo para instalar.

            Quando o trabalho for para o master, o APK definitivo sai em
            `segundoantes-apk-latest` — e esta prévia pode ser apagada.
          files: dist/segundoantes.apk
          fail_on_unmatched_files: true
