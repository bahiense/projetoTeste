<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Não há permissão de INTERNET aqui, e isso é de propósito: o programa
      inteiro vem dentro do APK e o que o usuário escreve no registro nunca
      sai do aparelho. Sem a permissão, nem um pedido de rede acidental
      conseguiria sair.
    -->

    <!-- Lembrete diário. A notificação precisa de permissão a partir do
         Android 13; o alarme no minuto certo, a partir do 12. Sem o alarme
         exato o aviso ainda chega, só que o sistema escolhe o minuto. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Alarme marcado não sobrevive a reiniciar o aparelho: é preciso
         remarcar no boot, a partir do plano guardado. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dispara o aviso e já remarca o dia seguinte. -->
        <receiver
            android:name=".AlarmeReceptor"
            android:exported="false" />

        <!-- Remarca depois de reiniciar o aparelho ou atualizar o app. -->
        <receiver
            android:name=".BootReceptor"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
