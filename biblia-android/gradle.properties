org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Cópia automática no Google Drive: ID do cliente OAuth (tipo Android) da sua
# própria conta Google. Vazio = recurso desligado, o resto do app igual.
#
# Como criar, uma vez só, de graça (console.cloud.google.com):
#   1. novo projeto
#   2. APIs e serviços -> ativar "Google Drive API"
#   3. tela de consentimento OAuth: tipo Externo, e publicar EM PRODUCAO
#      (em "Testes" o Google expira o acesso a cada 7 dias)
#   4. Credenciais -> criar ID do cliente OAuth -> Android
#        pacote:  com.bahiense.biblia
#        SHA-1:   D7:95:97:A7:59:00:42:A4:11:F0:DF:C7:22:4E:19:59:4D:1B:9F:FB
#   5. copie o ID aqui embaixo
#
# O escopo pedido e drive.file, que alcanca so os arquivos criados por este
# app. Por ser escopo nao sensivel, o Google nao exige verificacao do app.
driveClienteId=
